<Window x:Class="HomeWork13._7.OpenAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HomeWork13._7"
        mc:Ignorable="d"
        Title="OpenAccountWindow" Height="250" Width="300">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock>Выберите тип счета</TextBlock>
            <ComboBox Name="TypesBankAccounts">
                <TextBlock>Стандартный</TextBlock>
                <TextBlock>Не депозитный</TextBlock>
                <TextBlock>Депозитный</TextBlock>
            </ComboBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{DynamicResource ButtonDefult}" IsDefault="True" Click="Accept_Click">ОК</Button>
                <Button Style="{DynamicResource ButtonDefult}" IsCancel="True">Отмена</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
